# 날짜 미확인 이전 캘리브레이션

기존 `docs/calibration/`에 있던 사진, CSV, 카메라 기준 자료를 원본 그대로
이동했다. 현재 적용본이 아니며 과거의 `points.csv`가 남아 있다.
현재 9점 세션은 `experiments/2026-09-08/`에서 확인한다.

일부 기존 도구는 명시적 입력이 없을 때 이 폴더를 기본 입력으로 사용한다.
새 캡처 결과는 이 폴더에 덮어쓰지 않고 별도 세션으로 저장한다.

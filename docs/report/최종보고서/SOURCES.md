# 근거와 추가 확인 사항

기준일: 2026-09-10. 저장소 기준 커밋: `1fd914b`, 브랜치 `checkpoint/task1-calibration-fk-20260908`. 아래 경로는 별도 표시가 없으면 저장소 루트 기준이다. 이 문서는 보고서의 수치와 구현 설명을 추적하기 위한 편집 메모이며 제출 PDF에는 포함하지 않는다.

## 구성과 형식

| 자료 | 반영한 내용 |
|---|---|
| `docs/report/reference/example/최종보고서서식(워드).docx` | 서론 → 연구 배경 → 연구 내용 → 연구 결과 분석 및 평가 → 결론 및 향후 연구 방향; 영문 참고 문헌과 인용 순서 |
| `docs/report/reference/example/report_ko/pnureport.cls` | 변경 없는 학과 LaTeX 클래스. SHA-256 `a58ee24016d55b6e0397dcd95906bdcfe68fcb1b514c7c2fe386b85753158859` |
| `docs/report/착수보고서/`, `docs/report/중간보고서/` | 초기 목표, 장비 구성, 모방학습 준비, 구성원 정보. 초기 계획을 최종 구현으로 서술하지 않음 |
| `docs/report/25_(Sim2Real)삼성중공업_이재민_자문의견서.pdf` | 8월 3일 서면 자문. 4.6절에 항목별 반영과 남은 한계 정리 |
| `docs/report/Chats/`의 팀 대화(1446408419)와 공지 대화(1414022644) | 진행 일정, 역할, 시행 관찰, 시연 영상과 미션 제약. 팀 대화의 성공 표현은 독립적인 정량 평가와 구분 |
| 사용자 첨부 최종 제출 안내 이미지 | 지정 서식 유지, 목표·수행·결과·수정 이력, 역할·일정, 자문 대응. 시연 계획서 제출은 별도 절차 |

## 핵심 주장과 근거

| 보고서 내용 | 근거와 해석 범위 |
|---|---|
| ACT 단일 블록 이동 50회 중 29회, 58% | 이번 대화에서 사용자가 제공한 팀 집계. 계산은 29/50×100. 개별 시행 원자료는 제공되지 않았음. ACT와 CV+IK 비교 실험 또는 다섯 블록 전체 완주율이 아님 |
| 최종 CV+IK 검출·계획·실행 | `src/perception/detector.py`, `src/fsm/ik_handler.py`, `src/fsm/handlers.py`, `src/config.py`, `src/configs/default.yaml`, `src/control/` 및 태스크 러너. 실제 코드·설정을 우선하여 기술 |
| 설계 목표와 구현 차이 | `AGENTS.md`, `docs/architecture.md`와 코드 비교. 파지 직전 영상 상대 위치 보정, 모든 step의 짧은 실행, 내부 시간 예산을 구현 완료로 표현하지 않음 |
| 초기 ACT·VLA 검토와 전환 | 착수·중간보고서, 팀 대화, 기존 정책·실행 경로. 최종 런타임에서 ACT·VLA 호출 제외 |
| 9점 캘리브레이션 RMS 8.18mm, 최대 LOO 26.64mm | 아래 실험 디렉터리의 `teleop_ehdrms_ox0ydl6s/fit_accepted.txt`, `accepted_points.csv`. 채택 P1은 `P1_retry2`. 목표 RMS<5mm·최대 LOO<8mm 미달 |
| V1/V2의 19.94/7.11mm | 같은 세션 검증 자료. 서로 다른 위치의 값으로 개선 전후 비교가 아님 |
| 초기 드리프트 2.775px, 재시험 0.514px | 각각 `drift_60s.csv`, `stability_retry_Kjz2Pr/drift_60s.csv`의 **각 프레임 p95 중 최댓값**. 두 CSV 모두 약 60.5초까지 12행. 전체 관측의 통합 p95도, 10분 검증 결과도 아님 |
| 외부 180초 제한 시험 | `teleop_ehdrms_ox0ydl6s/task1_run.txt`와 세션 README. 네 번의 배치 동작 후 파란 블록이 남음. 종료 신호와 finally 정리 동작 완료는 구분. 슬롯 보정 override=0, 30Hz 조건을 현재 기본 설정과 구분 |
| 노란 블록 개별 파지 | `pick_validation/repeat/summary.json`, original/repeat 사진. original 파지 성공(위치 약21, 부하500), inner와 retry 빈손(위치 약2.49, 부하44). FK–목표 약1.85/2.13/1.42mm는 실제 턱–블록 오차가 아님 |
| 9월 2일 시연 | 팀 대화에서 20:45에 게시된 영상, 아래 미디어 항목 참조. 여러 시점의 정지 화면과 팀 설명을 확인. 영상 길이 약105.594초를 공식 미션 수행시간으로 간주하지 않음 |
| 적재 기능의 범위 | `StackPlaceStrategy`와 접촉 감지/하강 코드 존재. `src/configs/poses.yaml`에는 현재 home만 있음. 필요한 타워·접근 자세와 다섯 블록·5초 유지 실기 결과 미확보 |
| 기존 소프트웨어 시험 | 실험 아카이브의 로컬 94건·Orin 99건 기록. 이번 보고서 작성 중 다시 실행한 결과가 아니며 실제 미션 완주 증거가 아님 |

실험 디렉터리: `experiments/2026-09-08/session_20260908_AcC1p4/`.

카메라 드리프트 그래프는 원본 CSV, 캘리브레이션 그래프는 기록된 잔차·LOO 수치로 그렸다. 구조도·상태도·슬롯 배치는 코드와 설정의 설명 그림이며 센서 측정 결과가 아니다. 기본 임계값, 5mm 해제 여유, 보정량 등은 설정값이며 최적화 효과가 입증된 수치로 표현하지 않았다.

## 미디어 출처

`asset_manifest.json`에 각 파일의 원본 경로, 변환 방법과 SHA-256을 기록했다. 초기 장비 사진은 초기 구성임을 캡션에 명시했다. 파지 성공·실패 사진은 실험 아카이브 파일을 그대로 복사했다.

시연 원본:
`docs/report/Chats/KakaoTalk_Chats_2026-09-10_13.52.49_-1446408419/92e3f87c6652e3de778dfcedde65e76559a28a96f1e972d4d74f1bead563e7fe.mp4`

원본 HEVC 영상에서 FFmpeg로 0초·60초·104초 JPEG를 추출했다. 본문은 시작과 종료 부근의 0초·104초를 사용한다. 검토 시 15초 간격의 중간 프레임도 확인했지만 전체 영상의 모든 프레임을 판독한 평가는 아니다. 팔 가림과 원근이 있으므로 경계 통과·전체 성공률을 정지 사진으로 확정하지 않았다.

## 외부 참고 문헌

`references.bib`의 9개 항목은 SO-101 공식 문서, OpenCV의 HSV·형상·homography 문서, Modern Robotics의 수치 IK 설명, PlaCo 공식 문서, ACT 및 SmolVLA 원논문, LeRobotDataset 공식 문서이다. 작성 시 연결된 원문을 확인했다. 본문 첫 인용 순서로 번호를 부여하며 서지 정보는 영어로 유지했다. 외부 문헌의 성능을 팀 실험 결과로 전용하지 않았다.

참고 문헌의 URL은 웹 문서 7개에만 표시한다. arXiv 식별번호가 있는 ACT와 SmolVLA 논문은 중복 URL 표기를 생략했다. 원문 주소는 [ACT](https://arxiv.org/abs/2304.13705), [SmolVLA](https://arxiv.org/abs/2506.01844)이다.

## 제출 전 추가 확인

1. ACT 50회 집계의 시행 날짜, 체크포인트, 시작 조건, 성공 판정과 개별 기록. 현재는 사용자가 제공한 합계만 확정했다.
2. 최종 CV+IK의 무작위 다섯 블록 전체 미션 반복 평가. 성공/전체 횟수·제한시간·초기 배치·실패 단계가 필요하다.
3. 적재에 필요한 자세 파일과 실기 결과. 다섯 블록 및 5초 유지 조건 충족 여부는 아직 확정하지 않았다.
4. 카메라 10분 드리프트 조건과 독립적인 턱 위치 측정. 약 1분 재시험 결과를 확대 해석하지 않는다.
5. 현재 45Hz 등 최종 설정에서 전체 미션 재평가. 9월 8일 30Hz 시험과 동일 조건이라고 볼 수 없다.
6. 표지 연락처와 최종 제출 날짜의 팀 확인. 구성원별 역할은 9월 11일 사용자가 제공한 최신 분담 내용으로 갱신했다.
7. 시연 계획서의 장소, 장비, 네트워크 및 당일 실행 절차. 최종보고서와 별도로 제출해야 하며 이 초안 작성에는 제출을 포함하지 않는다.

추가 자료가 없는 항목은 현재의 한계 설명을 유지한다. 미확인 결과를 가상의 측정치로 채우지 않는다.

## 동료 초안 통합 반영 (2026-09-10)

검토한 동료 초안은 원본 체크아웃의 `docs/report/reference/졸과_최종보고서_김주환.pdf`이다. 업데이트본은 21쪽이며 SHA-256은 `e34d9ef15c2b5d7a6a324bf7ff01622cda20bf61b59357d63cea90980ef602bc`이다. 문제 관찰에서 설계 변경으로 이어지는 설명 방식과 개선 과제를 반영했다. 사진이나 실기 수치의 빈칸은 전용하지 않았다.

- ACT 정량 결과는 사용자 제공 29/50회(58%)로 통일했다. 동료 초안의 CV+IK 약 70%는 시행 수가 확인되지 않아 채택하지 않았다.
- 사용자가 추가로 확인한 캘리브레이션 순서: **5점 → 9점 → 15점 → 9점**. 초기 5점과 확장 9점의 RMS는 사용자가 2026-09-10 대화에서 실측값으로 확인한 **20.11mm**, **13.46mm**를 반영했다.
- 15점 RMS 11.98373823034977mm는 `experiments/2026-09-08/session_20260908_AcC1p4/archive/previous/venue_lab.json`의 `meta.num_points=15`, `meta.rms_mm`로 확인했다. 파일에는 2026-09-01 적합 시각이 기록되어 있다. 마지막 9점 RMS 8.18mm는 위 적합 보고서를 사용했다. 두 단계의 차이를 동일 조건의 개선율로 제시하지 않았다.
- 잔차 보간의 결과로 기구학 오차를 배제하거나 그리퍼 구속 부족을 원인으로 확정하는 주장은 넣지 않았다. 설계는 관찰한 잔여 오차에 대응한 것으로 설명한다.
- 손목 회전 변경 경위는 기존 `AGENTS.md`와 `docs/guide/SO101_CV_IK_파지운반.md`, 하강 중단 동작은 `src/control/trajectory.py` 및 같은 가이드에서 확인했다. 동료 초안의 하강 201회/31회 수치는 원자료 조건을 추가 확인하지 않아 새 정량 결과로 채택하지 않았다.
- 사용자가 적재 실기 미수행을 확인했다. 2차 미션은 구현 구조와 남은 실행·유지 검증으로 기술한다.
- 사용자가 보류한 설정값·그림의 세부 일치 검토는 이번 통합 범위에서 제외했다. 기존 보고서의 설정 표와 그림은 유지했다.

## 최종보고서 문체 정리 (2026-09-10)

본문의 초안 표시, 작성 기준 커밋·원본 파일 위치·채팅 검토 절차, 추가 확인 지시는 본 문서에서 관리한다. 표 3.1은 초기 접근법의 수행 내용과 결과, 표 4.1은 평가 항목과 방법, 표 4.5는 구현 및 수행 결과로 정리했다. 표 3.6의 항목형 역할 내용과 표 4.2의 대응점 수·RMS는 유지했다. 자문의견 표는 의견과 반영 내용으로 정리하고 반복 평가 계획은 5장으로 모았다.

- 코드 분석 기준은 계속 `1fd914b`이다. 보고서 브랜치의 main 기준과는 별개이며 본문에서 커밋 문자열만 생략했다.
- ACT 29/50회는 사용자 제공 팀 집계이다. 개별 로그와 시행 조건의 확인 항목은 위 목록에 유지한다.
- 초기 데이터 112개 에피소드는 중간보고서 단계의 집계이며, 현존 데이터셋 전체를 새로 검증한 총량이 아니다.
- 영상 길이 약105.594초, 표본 프레임 추출 시점 및 원본 경로는 위 미디어 항목을 따른다. 본문에는 시연 장면과 평가 범위만 남겼으며 공식 완료시간을 산출하지 않았다.
- 카메라 재고정 뒤 마지막 9점 세트의 채택 경로, 종료 신호와 실제 모터 정지의 차이, 30Hz 전체 미션 시험과 45Hz 단일 블록 시험의 구분은 기존 실험 자료를 따른다.
- 구현 설정과 계산 결과를 실측 성능으로 바꾸지 않았다. 초기 두 RMS는 이후 사용자 실측값으로 보완했으며, 마지막 캘리브레이션의 기준 미달, 제한시간 시험 미완료와 적재 실기 미수행은 본문에 유지했다.
- 이번 정리는 서술·표 구성·캡션·메타데이터에 한정한다. 학과 클래스, 그림·CSV 자산, 참고 문헌, 코드와 실제 실험 기록은 수정하지 않았다.

## 박사과정 연구생 피드백 반영 (2026-09-10)

사용자가 제공한 대화 기록 `/home/ehdrms/Downloads/Notes_260910_215620.txt`를 검토했다. SHA-256은 `dad9eb9dcdeef812e3d5af70347f9dbaa60d4ca6a89455d1462bf36f03aa9bb9`이다. 연구생 피드백을 팀원들이 논의한 전사 자료이며, 발화자와 고유명사의 인식이 불완전하므로 공식 공지나 실험 원자료로 취급하지 않는다. 원문은 제출 PDF에 포함하지 않는다.

- 212~220행의 후반 합의에 따라 학습 데이터 수집을 후속 구현 및 검토 계획으로 제시했다. 앞부분의 미구현 기능을 성과처럼 서술하자는 제안은 반영하지 않았다.
- 222~245행의 적재 논의는 2단/3단 실기 성공의 증거가 아니다. 4장에는 적재 실기 미수행을 유지하고, 5장에 높이별 도달성, 접촉과 해제, 다섯 블록의 5초 유지 평가 계획을 추가했다.
- 247~271행의 문제와 대응 중심 서술 및 성공/실패 분기 시각화, 296~302행의 접근법 전환 경위를 1장과 3장에 반영했다. 비교 자료가 없는 변경에는 정량 개선 효과나 단일 실패 원인을 추가하지 않았다.
- FSM 도식은 구현 기준 `1fd914b`의 `src/fsm/flows.py`, `ik_handler.py`, `handlers.py`, `task1.py`, `src/control/grasp.py`, `sensing.py`와 `src/configs/default.yaml`을 따른다. PICK 내부 후보 시도와 VERIFY 재확인을 구분했다. 위치 12, 부하 200은 현재 설정이며 센서 판정의 정답률을 뜻하지 않는다. 1차 미션의 새 프레임 기반 완료 판정, 순회별 보류와 외부 시간 제한도 유지했다.
- 기존 `src/camera/recorder.py`는 사진 저장 기능이다. CV+IK 실행의 영상/관절/명령 동기화, 에피소드 구성, 학습용 라벨 및 보상 저장을 구현한 것으로 확대하지 않았다. 5장의 해당 도식과 표는 제안하는 설계이다.
- 모방학습과 VLA 활용을 배제하지 않고 영상, 상태, 행동 표현과 작업 지시의 호환 조건을 설명했다. [LeRobotDataset 공식 문서](https://huggingface.co/docs/lerobot/lerobot-dataset-v3)와 [SmolVLA 공식 문서](https://huggingface.co/docs/lerobot/smolvla)를 확인했다. 최신 데이터 문서 인용은 기존 환경을 새 버전으로 이행했다는 뜻이 아니며, 구현할 때 저장 형식과 고정된 라이브러리 버전의 호환성을 확인해야 한다.
- 데이터 수집의 첫 단계는 사람의 블록 재배치를 포함한다. 파지 확인, 배치 결과, 시간 초과를 구분하고, 자동 라벨과 사람의 영상 판정을 비교하는 평가를 계획했다. 학습 성능 향상, 사람 개입 시간 감소 또는 무인 반복 수집량은 측정 결과로 제시하지 않았다.
- 학과 서식과 기존 실험 수치는 유지했다. 8월 3일 산업체 서면 자문과 이번 연구생 피드백은 서로 다른 자료로 관리한다.

## 결과 중심 개정과 원본 로그 재집계 (2026-09-10)

사용자가 전달한 외부 검토와 짧은 추가 시험만 가능하다는 조건에 따라, 기존 자료의 결과 집계와 실패 시간 분석을 먼저 보강했다. 이번 작업에서 새로운 하드웨어 시험을 수행하지 않았다.

- 초록에 실제 수행 범위와 미달 항목을 요약했다. 공식 제목과 학과 클래스는 유지했다. ACT 29/50회는 개발 경위에 한정하고 최종 CV+IK의 성공률로 전용하지 않았다.
- 4장 첫 표를 실기 시험의 조건과 결과 요약으로 바꾸었다. 9월 2일 시연, 9월 8일 30Hz 전체 미션 및 이후 45Hz 단일 블록 시험을 같은 조건의 반복으로 합치지 않았다.
- `evidence/20260908/`에 원본 로그, 채택 CSV, 저장 H, 적합 결과와 세션 README를 그대로 복사했다. 출처 해시는 `analysis/source_hashes.json`, 재현 방법은 `analysis/README.md`에 있다.
- `task1_run.txt`의 후보 시작은 14회, 센서 HELD 4회, EMPTY 9회, 판정 전 중단 1회다. PICK 진입은 빨간 블록 재선택을 포함하여 6회다. VERIFY에서 운반으로 전이한 4회와 슬롯 해제 4회를 별도 집계했으며, 물리적 파지/배치 성공과 독립된 센서/동작 기록으로 표현했다.
- 다섯 대상의 최초 후보 중 HELD는 2회다. 노랑은 같은 PICK의 추가 후보에서, 빨강은 재선택한 PICK에서 HELD를 얻었다. 추가 후보 시작은 9회이며 이 중 1회는 중단되었다. 계획 단계에서 제외된 후보와 VERIFY 재확인은 파지 실행 횟수에 포함하지 않았다.
- 빨간 블록의 최초 PICK은 51.492초, 최초 선택부터 재선택 뒤 슬롯 해제까지는 69.815초다. 파란 블록은 두 차례 EMPTY 뒤 세 번째 후보의 접근 중 중단됐다. 시간축은 미션 시작 로그 `03:10:05.080` 기준이며, 외부 180초 제한은 초기화도 포함한다. 마지막 동작 로그 뒤의 중단 시각과 물리적인 정지 시각은 추정해 채우지 않았다.
- 상태 흐름 그림은 위 로그에서 생성했다. `placed 0` 필드를 인정 블록 수로 사용하지 않았고, `released_slot` 전이도 인정 블록 수와 구분했다. `DONE` 전이는 없으며, 미완료 판단은 세션 README의 마지막 블록 잔류 관찰과 함께 해석했다.
- 좌표 그림은 저장 H를 원본 픽셀 대응점에 적용하여 FK 좌표와의 차이를 실제 크기로 표시했다. 재계산 RMS는 8.17677572456157mm로 저장값과 일치한다. 재적합이나 점 제외는 하지 않았고, 기존 LOO 표는 그대로 유지했다. 높이 1.2–26.3mm는 FK 계산값이며 독립 측정한 물리적 접촉 높이로 표현하지 않았다.
- 새 관측 사진은 `zone_preview.png`의 사본으로, 당시 등록한 영역과 슬롯 오버레이를 포함한다. 전체 미션 초기 배치나 결과 사진은 아니다. 기존 시연/파지 사진은 원본 바이트를 유지하면서 LaTeX의 `trim,clip`으로 관심 영역을 확대했다. 확대만으로 경계 인정이나 센서 정답률을 새로 확정하지 않았다.
- 모듈 표를 외부 기술과 팀의 구현 구분으로 정리하고, 자문 표의 문서 편집 이력 표현을 설계/평가 판단으로 바꾸었다. RMS/LOO 개발 목표와 IK 후보 허용값을 구분했으며, 실측 파지 여유에서 도출한 기준으로 소급하지 않았다.
- `1fd914b`는 후속 구조 정리 및 구현 설명의 기준이다. 원기록에 없는 실기 당시 커밋, 카메라 모델/설치 치수와 패키지 버전을 추정해 채우지 않았다.
- 학습 데이터 수집 계획은 두 문단으로 압축했다. `evaluation/`에는 같은 배치의 추가 미션 3회와 센서/영상 대조를 위한 절차, 시행/후보 CSV 헤더만 제공한다. 새 결과 행과 성공률은 작성하지 않았다.

## 구성원 역할 갱신 (2026-09-11)

사용자가 김주환, 윤민석, 이동근의 최신 역할 목록을 직접 제공하여 표 3.6과 역할 분담 도입 문장을 갱신했다. 기존 추정 역할 목록보다 이번 명시적 분담을 우선했다. 각 항목은 명사형으로 유지하고 구분에는 쉼표와 슬래시를 사용했다.

- 김주환: ACT/SmolVLA 시도와 한계 분석, 전환 근거 문서화, 정량 평가 체계와 rollout 템플릿, 캘리브레이션 시행착오 공유, 환경 통합 테스트와 rollout 검증, PR/기술 문서 정리, 최종보고서와 시연 PPT 작성.
- 윤민석: 비전 검출 조건과 영역 필터, mm 단위 위치 보정과 Roll 정렬, 손목 과열 분석과 제어 개선, 파지 재시도와 운반, 미션 종료 및 1차 미션 통합, 포스터 제작.
- 이동근: FSM 대체 파지 후보 로직 보완, 웹 관제와 검출/후보/FK 시각화, 캘리브레이션 기록과 재개 도구, 영상 변화 기반 저장, LeRobot/uv/CLI 환경 정리, 실험 자료와 중간/최종보고서 작성.
- 사용자의 '1차 미션 통합 완성'은 역할 표에서 '1차 미션 제어 경로 통합'으로 표현했다. 구현 기여를 명시한 것이며 기존 제한시간 시험을 미션 성공으로 재판정하지 않았다. 역할 갱신을 근거로 적재 실기 결과나 rollout 성능 수치를 추가하지 않았다.

## 추가 회의록 참조 (2026-09-11)

`/home/ehdrms/Downloads/새로운 녹음 28_전사본.txt`의 923행 전체를 확인했다. SHA-256은 `756d3d894f1008f4f4c0a45e8eff3ca5e93ef1797e03292825f1dd6a00cfc6da`이다. 전문 용어 오인식과 화자 구분 부재를 전제로 검토했으며, 실제 회의 날짜와 공식 발표/제출 일정은 이 자료로 확정하지 않았다.

시간대별 논의, 현재 보고서와의 대응, 수치의 해석 경계와 발표 메모를 [추가 회의록 검토](MEETING_REVIEW_20260911.md)에 정리했다. 문제/원인/대응의 시각화와 팀 기여 구분은 기존 본문에 반영되어 있어 유지했다. 5.2.3에는 강화학습용 데이터의 다음 관측, 보상과 종료 조건을 정의해야 한다는 확장 조건을 보강했다.

구두 추정 약 70%, 적재 높이별 자세 가능성, 24시간 자동 수집 제안은 새 실측 성과로 추가하지 않았다. 전사의 재시도 10mm와 코드의 추가 후보 15mm가 다른 점도 기록했다. 이번에는 코드/실험 결과/역할 표를 바꾸지 않았고, 새 실기 시험이나 발표 자료 배포를 수행하지 않았다.

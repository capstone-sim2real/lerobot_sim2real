\chapter{서론}
\section{연구 배경}
본 과제의 목표는 SO-ARM101 로봇팔로 다섯 블록을 제한시간 안에 지정 영역으로 옮기고, 후속 미션에서 적재하는 것이다. 이하에서는 SO-ARM101을 SO-101로 표기한다. 초기에는 이동형 로봇의 물체 수집을 검토했으나 과제 규격이 바뀌면서 SO-101 블록 조작으로 범위를 변경하였다. 개발 환경에서 개별 동작을 성공시키는 것과 함께, 장비를 실제 시연장으로 옮긴 뒤에도 여러 블록을 반복 처리할 수 있어야 했다.

초기 접근은 사람이 리더암으로 시연한 동작을 ACT와 SmolVLA로 학습하는 것이었다. 시연 수집·학습·원격 추론 환경을 구성하고 ACT 데이터를 131개 에피소드까지 늘렸으나, 작업 범위와 관측 조건이 달라졌을 때 안정적인 파지를 확보하기 어려웠다. 실패한 동작을 관찰해도 데이터, 정책과 실제 접촉 중 어느 부분을 수정해야 할지 판단하기 어려워 반복적인 개선에 시간이 소요되었다.

팀은 남은 개발 기간과 시연장 운용 조건을 고려하여, 실패 원인을 단계별로 확인하고 현장에서 조정할 수 있는 구조가 필요하다고 판단하였다. 크기와 색상이 정해진 블록과 고정된 작업 평면이라는 조건을 활용해 컴퓨터 비전과 역기구학(CV+IK)으로 전환하였다. 검출 위치, 좌표 변환, IK 도달성, 그리퍼 위치·부하와 상태 전이를 각각 확인하도록 구성하였다. CV+IK에서도 카메라 고정과 재캘리브레이션, 조명에 따른 검출 조건 조정은 필요하며, 환경 영향을 점검할 수 있는 경계를 명시하는 것이 설계의 핵심이었다.

\begin{figure}[htbp]
\centering
\begin{tikzpicture}[
box/.style={draw,rounded corners,text width=11.5cm,minimum height=12mm,align=center,font=\small},
arr/.style={-{Latex},thick}]
\node[box] (learn) {ACT / SmolVLA 실험\\시연 수집, ACT 131에피소드 확대, 추론 설정 조정};
\node[box,below=12mm of learn] (cvik) {CV+IK로 전환\\검출·좌표·IK·파지 확인을 분리하여 진단과 보정};
\node[box,below=12mm of cvik] (result) {실기 개선과 확장\\1차 미션 반복 수행, 3·4단 적재 관찰, Task3 자동 수집};
\node[box,dashed,below=10mm of result] (future) {후속 검증: 최종 적재 높이와 유지, 5단 달성, 수집 자료의 재학습};
\draw[arr] (learn)--node[right,font=\scriptsize]{환경 민감성과 실패 진단의 어려움}(cvik);
\draw[arr] (cvik)--node[right,font=\scriptsize]{단계별 실패 분석과 변경}(result);
\draw[arr,dashed] (result)--(future);
\end{tikzpicture}
\caption{모방학습 실험에서 CV+IK와 실행 데이터 수집으로 이어진 개발 과정. 점선은 아직 검증하지 않은 후속 과제이다.}
\label{fig:research-path}
\end{figure}

\section{초기 접근의 한계와 설계 요구}
첫째, 시연 수집 환경과 실행 환경의 일관성이 필요하였다. 카메라 위치·각도나 조명 등 관측 조건이 달라지면 동작 결과가 변하는 현상을 관찰하였다. 당시 결과만으로는 실제 시연장에서 정책의 재현성을 충분히 신뢰하기 어려웠다. 환경 변화를 고려한 일반화 성능은 별도 통제 실험으로 측정하지 않았으며, 이 관찰을 설계 선택의 근거로 사용하였다.

둘째, 실패 원인을 구분할 수 있어야 했다. 팔이 목표 근처에 도달해도 블록을 잡지 못했을 때, 영상 입력과 행동 출력, 기구 오차와 접촉 상태가 함께 작용할 수 있었다. 최종 시스템에서는 검출 중심, 로봇 좌표, 목표 관절 자세와 센서 판정을 각각 기록하여 수정할 단계를 좁히도록 하였다.

셋째, 개별 파지 실패를 전체 미션의 중단으로 이어지지 않게 해야 했다. 제한시간 내 여러 블록을 처리하려면 파지를 확인한 뒤 운반하고, 실패 시 재시도하거나 대상을 다시 선택해야 한다. 최종 제어 흐름은 이 판단을 명시적으로 관리하며, 모방학습 실험에서 사용한 모터 통신과 장비 운용 경험도 재사용하였다.

이후 개발에서는 색상이 비슷한 테이프의 오검출, 영상 좌표와 실제 파지 위치의 편차, 빈손 파지와 재시도 시간 비용을 다루었다. 3장은 이에 대응한 최종 구조를 설명하고, 4장은 개선 후 수행 결과와 초기 실패의 근거를 연결한다.

\section{연구 목표}
\subsection{미션 규격과 성공 조건}
블록은 $40\times40\times20\,\mathrm{mm}$ 크기이며, 서로 다른 색의 블록 다섯 개를 사용한다. 미션 시작 시 각 블록은 넓은 면을 바닥에 둔 채 지정 영역 밖에 놓인다. 블록끼리 맞닿을 수는 있지만 위아래로 겹치지는 않는다. 두 미션은 블록을 다시 배치한 뒤 각각 수행한다. 목표와 평가 조건은 표~\ref{tab:mission}과 같다.

\begin{table}[htbp]
\centering
\caption{과제 미션의 목표와 평가 조건}\label{tab:mission}
\begin{tabularx}{\linewidth}{@{}L{0.16\linewidth}YY@{}}
\toprule
항목 & 1차 미션: 이동 & 2차 미션: 적재 \\
\midrule
목표 & 다섯 개 블록을 지정 영역에 배치 & 다섯 개 블록을 수직으로 적재 \\
제한시간 & 180초 & 300초 \\
지정 영역 & \multicolumn{2}{l}{$200\times100\,\mathrm{mm}$ 내부 영역} \\
인정 조건 & 경계 20\,mm 이내 걸침, 세로 세움과 일부 겹침 허용. 적재는 불인정 & 적재 상태를 5초 이상 안정적으로 유지 \\
부분 수행 & 시간 종료 시 영역에 놓인 블록 수 & 시간 종료 시 적재한 블록 수 \\
\bottomrule
\end{tabularx}
\end{table}

지정 영역은 로봇 베이스에서 약 25\,cm 떨어진 곳에 두며, 제어할 때는 네 꼭짓점을 로봇 베이스 좌표계에 등록한다.

신경망은 최대 하나까지 사용할 수 있으나 필수는 아니다. 최종 CV+IK 시스템은 신경망 없이 영상 처리와 기구학 계산으로 동작한다.

\subsection{세부 연구 목표}
영상에서 지정 영역 밖의 블록을 골라 로봇 좌표로 변환하고, 관절 한계와 그리퍼 방향을 고려한 접근·파지·이동 자세를 생성한다. 파지 확인과 재시도를 포함한 상태 흐름으로 블록을 반복 처리하며, 단계별 자료를 기록해 실패 지점을 추적한다. 적재 동작과 실행 자료의 학습 데이터 변환도 확장 대상으로 삼았다.

평가는 초기 진단 시험, 캘리브레이션 재수집, 개선 후 반복 미션과 적재 시험, 자동 데이터 수집으로 나누었다. 개선 후 1차 미션은 30회 중 28회(93.3\%) 완료하였다. 2차 미션은 5단을 목표로 20회 시도했으며 수행 중 3단에는 20회, 4단에는 17회 도달했다. 5단 도달과 종료 후 유지라는 전체 규격은 달성하지 못하였다. Task3은 성공 실행의 자동 기록을 구현하고 첫 라운드 5개 에피소드를 저장하였다. 실험 조건과 집계의 적용 범위는 4장에서 구분한다.
\FloatBarrier
